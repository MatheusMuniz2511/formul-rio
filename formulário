<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Inscrição SENAI</title>
  <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300;400;500;600;700&display=swap" rel="stylesheet" />

  <style>
    /* ====== RESET E BASE ====== */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Quicksand', sans-serif;
    }

    body {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      background: #000;
      overflow: hidden;
    }

    /* ====== FUNDO ANIMADO ====== */
    section {
      position: fixed;
      width: 100vw;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-wrap: wrap;
      gap: 2px;
      overflow: hidden;
      z-index: 1;
    }

    section::before {
      content: '';
      position: absolute;
      width: 100%;
      height: 100%;
      background: linear-gradient(#000, rgb(25, 0, 255), #000);
      animation: animate 5s linear infinite;
      z-index: 0;
    }

    @keyframes animate {
      0% { transform: translateY(-100%); }
      100% { transform: translateY(100%); }
    }

    section span {
      position: relative;
      display: block;
      width: calc(6.25vw - 2px);
      height: calc(6.25vw - 2px);
      background: #181818;
      z-index: 2;
      transition: 1.5s;
    }

    section span:hover {
      background: rgb(25, 0, 255);
      transition: 0s;
    }

    /* ====== FORMULÁRIO ====== */
    .signin {
      position: relative;
      z-index: 1000;
      background: #222;
      width: 90%;
      max-width: 400px;
      padding: 40px;
      border-radius: 6px;
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.9);
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .signin h2 {
      font-size: 2em;
      color: rgb(0, 204, 255);
      text-transform: uppercase;
      margin-bottom: 20px;
      text-align: center;
    }

    .form {
      width: 100%;
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .inputBox {
      position: relative;
      width: 100%;
    }

    .inputBox input,
    .inputBox select,
    .inputBox textarea {
      width: 100%;
      background: #333;
      border: none;
      outline: none;
      padding: 25px 10px 7.5px;
      border-radius: 4px;
      color: #fff;
      font-weight: 500;
      font-size: 1em;
    }

    .inputBox textarea {
      resize: none;
      height: 80px;
    }

    .inputBox label {
      position: absolute;
      left: 10px;
      top: 15px;
      color: #aaa;
      font-size: 1em;
      pointer-events: none;
      transition: 0.4s;
    }

    /* Label flutuante ao focar/preencher */
    .inputBox input:focus + label,
    .inputBox input:valid + label,
    .inputBox select:focus + label,
    .inputBox select.filled + label,
    .inputBox textarea:focus + label,
    .inputBox textarea:valid + label {
      top: 5px;
      font-size: 0.8em;
      color: rgb(0, 204, 255);
    }

    /* ====== BOTÃO COM ANIMAÇÃO ====== */
    input[type="submit"] {
      position: relative;
      background: rgb(0, 204, 255);
      color: #000;
      font-weight: 600;
      font-size: 1.1em;
      letter-spacing: 0.05em;
      border: none;
      border-radius: 6px;
      padding: 12px 20px;
      cursor: pointer;
      overflow: hidden;
      transition: all 0.3s ease;
      box-shadow: 0 5px 15px rgba(0, 204, 255, 0.4);
    }

    input[type="submit"]::before {
      content: '';
      position: absolute;
      top: 0;
      left: -75%;
      width: 50%;
      height: 100%;
      background: rgba(255, 255, 255, 0.4);
      transform: skewX(-25deg);
      transition: all 0.5s ease;
    }

    input[type="submit"]:hover::before {
      left: 125%;
    }

    input[type="submit"]:hover {
      background: rgb(0, 230, 255);
      transform: scale(1.05);
      box-shadow: 0 0 20px rgb(0, 230, 255), 0 0 40px rgba(0, 230, 255, 0.6);
    }

    /* ====== RESPONSIVIDADE ====== */
    @media (max-width: 900px) {
      section span {
        width: calc(10vw - 2px);
        height: calc(10vw - 2px);
      }
    }

    @media (max-width: 600px) {
      section span {
        width: calc(20vw - 2px);
        height: calc(20vw - 2px);
      }
    }
  </style>
</head>
<body>

  <section id="background"></section>

  <div class="signin">
    <h2>Inscrição SENAI</h2>
    <form class="form" action="https://www.pe.senai.br/" method="post">
      <div class="inputBox">
        <input type="text" name="nome" id="nome" required />
        <label for="nome">Nome completo</label>
      </div>

      <div class="inputBox">
        <input type="email" name="email" id="email" required />
        <label for="email">E-mail</label>
      </div>

      <div class="inputBox">
        <input type="tel" name="telefone" id="telefone" required />
        <label for="telefone">Telefone</label>
      </div>

      <div class="inputBox">
        <select name="curso" id="curso" required>
          <option value="" disabled selected hidden></option>
          <option value="informatica">Informática</option>
          <option value="mecanica">Mecânica</option>
          <option value="eletrica">Elétrica</option>
          <option value="logistica">Logística</option>
          <option value="automacao">Automação Industrial</option>
        </select>
        <label for="curso">Curso desejado</label>
      </div>

      <div class="inputBox">
        <textarea name="observacoes" id="observacoes" required></textarea>
        <label for="observacoes">Observações</label>
      </div>

      <div class="inputBox">
        <input type="submit" value="Enviar Inscrição" />
      </div>
    </form>
  </div>

  <script>
    // Cria dinamicamente os blocos do fundo animado (sem document.write)
    const background = document.getElementById("background");
    for (let i = 0; i < 400; i++) {
      const span = document.createElement("span");
      background.appendChild(span);
    }

    // Faz o label do select "subir" quando um curso é selecionado
    const selectCurso = document.getElementById('curso');
    selectCurso.addEventListener('change', function() {
      this.classList.toggle('filled', this.value !== "");
    });
  </script>

</body>
</html>